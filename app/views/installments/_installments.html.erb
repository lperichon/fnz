<ul class="installments">
  <% installments.each do |installment| %>
      <li>
        <%= installment.due_on %> <%= link_to "Show", business_membership_installment_path(@business, @membership, installment) %> | <%= link_to "Edit", edit_business_membership_installment_path(@business, @membership, installment) %> | <%= link_to 'Delete', business_membership_installment_path(@business, @membership, installment), :confirm => 'Are you sure you want to delete this item?', :method => 'delete' %>
      </li>
  <% end %>
</ul>