<% content_for :sidebar do %>
    <%= render :partial => 'businesses/edit_sidebar' %>
<% end %>

<ul class="agents">
  <% @agents.each do |agent| %>
    <li>
      <%= link_to agent.name, business_agent_path(@business, agent) %>
    </li>
  <% end %>
</ul>

<br />

<%= link_to t("agents.index.#{@business.type.downcase}.new_agent"), new_business_agent_path(@business) %>