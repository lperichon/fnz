<h3><%= _("Cierres de caja '%{account}'") % {account: @account.name} %></h3>

<table class="table table-striped table-hover table-condensed table-bordered">
  <% @balance_checks.each do |bc| %>
    <tr class="balance_check_<%= bc.id %>">
      <td><%= l bc.checked_at, format: :short %></td>
      <td><%= bc.balance %></td>
      <td><%= bc.creator.name %></td>
      <td>
        <%= link_to content_tag(:i, "", class: "icon-list"),
                    business_account_balance_check_path(@business,@account,bc) %>
        <%= link_to content_tag(:id,"",class: "icon-trash"),
                    business_account_balance_check_path(@business,@account,bc),
                    method: :delete,
                    confirm: _("¿seguro?"),
                    remote: true %>
      </td>
    </tr>
  <% end %>
</table>

<%= link_to _("volver a %{account}") % { account: @account.name}, business_account_transactions_path(@business,@account) %>
