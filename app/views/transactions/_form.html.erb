<%= simple_form_for(@transaction, :as => :transaction, :url => form_url) do |f| %>

    <%= render :partial => 'fields', :locals => {:f => f} %>

    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>