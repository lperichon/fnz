<%= simple_form_for(@transaction, :as => :transaction, :url => form_url, :validate => true, :html => {:class => "form-horizontal", :data => { :controller => "transaction_form" }}) do |f| %>

    <%= render :partial => 'fields', :locals => {:f => f} %>

    <%= f.button :submit, :class => "btn-primary" %>
<% end %>
