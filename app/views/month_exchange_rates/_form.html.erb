<%= simple_form_for(@month_exchange_rate,
      validate: true,
      url: form_url,
      html: {class: "form-horizontal"}
    ) do |f| %>

  <%= f.input :ref_date,
    as: :date,
    discard_day: true,
    input_html: {
      disabled: !@month_exchange_rate.change_allowed?(:report_at)
    }  %>

  <%= f.input :from_currency_id, as: :select, collection: SUPPORTED_CURRENCIES %>
  <%= f.input :to_currency_id, as: :select, collection: SUPPORTED_CURRENCIES %>

  <%= f.input :conversion_rate %>


  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>