<%= form_for(@tag, :url => form_url) do |f| %>
    <% if @tag.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@tag.errors.count, "error") %> <%= _("evitaron que la categoría sea actualizada:") %></h2>

          <ul>
            <% @tag.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <p>
      <%= f.label :name %><br />
      <%= f.text_field :name %>
    </p>

    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
