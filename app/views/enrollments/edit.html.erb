<% content_for :sidebar do %>
    <%= render :partial => 'businesses/sidebar' %>
<% end %>

<h2>Edit Enrollment</h2>

<%= render :partial => 'form', :locals => {:form_url => business_membership_enrollment_path(@business, @membership, @enrollment)} %>

<%= link_to 'Show', business_membership_enrollment_path(@business, @membership, @enrollment) %> |
<%= link_to 'Back', business_membership_path %>