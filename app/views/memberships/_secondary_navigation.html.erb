<li class="pull-right">
<% if current_user.overview_mode == 'list' %>
  <%= link_to content_tag(:i, "", :class => "icon-table"), params.merge(:overview_mode => 'table') %>
<% else %>
  <%= link_to content_tag(:i, "", :class => "icon-list"), params.merge(:overview_mode => 'list') %>
<% end %>
</li>
<li class="pull-right">
  <%= link_to content_tag(:i, "", :class => "icon-plus") + t('memberships.secondary_navigation.new_membership'), new_business_membership_path(@business) %>
</li>

